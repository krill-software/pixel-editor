name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  release:
    uses: krill-software/.github/.github/workflows/krill-app-release.yml@main
    with:
      product-name: "Pixel Editor"
      example-file: "sprite.png"
    secrets: inherit
    permissions:
      contents: write
